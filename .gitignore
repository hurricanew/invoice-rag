.env
.env.local
node_modules/
cdk.out/
dist/
coverage/
*.log
.DS_Store

# Local Stage A persistence — run state should not be committed
data/
data-test-*/

# Generated from finance_rag_corpus/ via src/lib/ingestCorpus.ts — reproducible
# (re-running ingestion recomputes both; the cache just avoids repeat Bedrock
# calls for unchanged content), not committed
fixtures/corpus_chunks.json
fixtures/corpus_embeddings_cache.json

# Contains real account ID / guardrail ID in resource ARNs — not for public commit
iam/interviewer-bedrock-policy.json

# Original assignment PDF — not ours to redistribute in a public repo
Agentic_AI_Engineer_Take_Home_Test.pdf
